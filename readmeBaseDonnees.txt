Comme j'ai testé la connection, j'ai utilisé mon compte.
Du coup on peut utiliser mon compte pour le projet. le nom est jaylesr et le mot de passe est jaylesr. Ces informations sont dans le code de toute manière

Une fois connecté avec SQLPLUS, on peut lister toutes le tables avec :
SELECT table_name FROM all_tables

(il y a pas mal de trucs qui servent à rien)

Pour connaitre le schéma de la table, on peut utiliser :
DESCRIBE nom_de_la_table
